---
import type { Lang } from '../i18n/ui';
import { useTranslations } from '../i18n/utils';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<section class="min-h-screen grid md:grid-cols-2">
  <!-- Left: text content -->
  <div class="flex flex-col justify-center px-8 lg:px-16 xl:px-24 pt-40 pb-24 md:pt-0 md:pb-0">
    <p class="text-[0.85rem] uppercase tracking-[0.25em] text-accent font-light mb-10">
      {t('hero.subtitle')}
    </p>
    <h1 class="font-serif text-[clamp(2.8rem,5vw,4.5rem)] font-bold text-primary leading-[1.08] mb-10">
      {t('hero.title')}
    </h1>
    <p class="text-lg text-text-light leading-loose max-w-md mb-14">
      {t('hero.tagline')}
    </p>
    <a
      href="#about"
      class="group inline-flex items-center gap-3 self-start px-10 py-4 border border-primary/10 text-sm font-light tracking-wide text-primary rounded-sm hover:bg-primary hover:text-white transition-all duration-500"
    >
      {t('hero.cta')}
      <span class="transition-transform duration-300 group-hover:translate-x-1">&rarr;</span>
    </a>
  </div>

  <!-- Right: image area -->
  <div class="bg-bg-alt flex items-center justify-center relative overflow-hidden">
    <div class="absolute inset-0 bg-bg-alt"></div>
    <!-- Replace with: <img src="/anna-photo.jpg" alt={t('hero.title')} class="relative z-10 w-full h-full object-cover" /> -->
    <span class="relative z-10 text-text-light text-sm">Profilkép / városi kép helye</span>
  </div>
</section>
